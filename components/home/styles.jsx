import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  modalTitle: {
    fontSize: 20,
    marginBottom: 20,
    fontWeight: 'bold',
  },
  workoutItem: {
    borderColor: '#ccc',
    borderWidth: 1,
    borderRadius: 4,
    marginTop: 15,
    padding: 10,
  },
})

export default styles
